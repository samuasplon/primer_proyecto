<?php

/**
 * Implementing hook_theme().
 */
function libro1_theme($existing, $type, $theme, $path) {

  return array(
    'article_description' => array(
      'variables' => array('items' => array(), 'title' => '')
    )
  );
}
